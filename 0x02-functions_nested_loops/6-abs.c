#include "main.h"

/**
 * _abs - computes the absolute value of int n
 * @n: the integer used in computation
 * Return: the absolute value of int n
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
